


const Book = (props) => {
    return <div>
        <h1>{props.title}</h1>
        <span>{props.author}</span>
        <span>{props.author}</span>
        <span>{props.author}</span>
        <span>{props.author}</span>

    </div>
}